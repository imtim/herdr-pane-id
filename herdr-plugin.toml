id = "pane-id"
name = "Pane ID"
version = "0.1.0"
min_herdr_version = "0.8.0"
description = "Label every newly created pane with its pane ID: UI title, prompt prefix, and a one-line banner"
platforms = ["linux", "macos"]

[[events]]
on = "pane.created"
command = ["bash", "on-pane-created.sh"]
